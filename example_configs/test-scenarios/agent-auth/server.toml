#:schema ../../../scenario-schema.json

# Shared test server config — SSH agent auth scenarios inherit from this.
# Requires ssh-agent running with the test key loaded:
#   eval $(ssh-agent)
#   ssh-add ../key-auth/test_key

[credentials]
username = "test_user"

[server]
host = "localhost"
port = 2222
