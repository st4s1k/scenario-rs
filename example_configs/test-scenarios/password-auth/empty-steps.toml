#:schema ../../../scenario-schema.json

# Test: Empty steps list
# UI: "No steps to display" empty state

parent = "./server.toml"

steps = []
[tasks.remote_sudo.unused_task]
command = "echo 'unused'"
description = "This task is never referenced"
error_message = "Unused"
