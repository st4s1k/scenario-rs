[default]
workspace = true
skip-clean = true

exclude-files = [
    # Entry points (runtime glue, not testable)
    "cli/src/main.rs",
    "gui/src-tauri/src/main.rs",
    # Tauri command handlers (pure delegation to ScenarioAppStateCore)
    "gui/src-tauri/src/commands.rs",
]

[report]
out = ["Html"]
